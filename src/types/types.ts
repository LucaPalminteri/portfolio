export type Work = {
    title: string,
    position: string,
    duration: string,
    description: string
}

export type Blog = {
    title: string,
    views: number
}

export type Project = {
    title: string,
    description: string,
    image: string,
    link: string
}

export type Tag = {
    title: string,
    image: string,
    color: string
}